{
  "name": "ai-vision-dialogue",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@11.2.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.3",
    "typescript": "catalog:"
  }
}
